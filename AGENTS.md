# 이 저장소에서 지킬 규칙

사람과 AI 에이전트 모두에게 해당한다. 짧게 유지한다.

## 1. 수치는 출처 없이 코드에 넣지 않는다

이 파이프라인이 만드는 블록아웃은 **Seedance 의 출력을 강제한다.** 지오메트리가
틀리면 프롬프트에 뭐라고 쓰든 지오메트리가 이긴다. 그래서 치수 하나하나가 결과물의
사실관계다.

치수는 두 종류뿐이고 코드에서 구분되어야 한다.

- **실측** — 도면·사진 계측·문헌값. 사실이다.
- **가정** — 그 외 전부. 그럴듯해 보여도 추측이다.

`assumptions.py` 의 `A()` 로 등록한다. `source` 는 키워드 전용 필수 인자라
빠뜨릴 수 없고, 모르면 `source=None` 이라고 써야 한다.

```python
COL_D = A("COL_D", 0.21, "m", "기둥 한 변(방주)",
          source="민가는 방주만 허용 — 조선시대 원기둥 금지. 6~8치의 중간값")
KAN   = A("KAN", 2.4, "m", "1칸(주간거리)", source=None)   # 미확인
```

`source=None` 인 값은 생성기를 돌릴 때마다 **미확인 목록으로 화면에 뜨고**
사이드카 마크다운에도 `**미확인 (추측)**` 으로 남는다. 주석 뒤에 숨을 수 없다.
`--strict-assumptions` 로 돌리면 미확인이 하나라도 있을 때 exit 1.

## 2. 주석을 출처처럼 쓰지 않는다

```python
TOEN_D = 0.9   # 민가는 반 칸 안팎        ← 금지. 지어낸 값을 사실로 위장한다
```

이건 실제로 커밋된 적이 있다. 반 칸은 1.2m 라 주석과 숫자조차 맞지 않았고,
다음에 읽는 사람은 그걸 검증된 값으로 읽는다. 출처를 적을 자리는 `A(source=...)`
하나뿐이다.

## 3. 확인할 수 있으면 확인하고, 못 하면 물어본다

- 좌표·크기·색은 **재서** 말한다. 렌더 결과는 **프레임을 뽑아 눈으로** 본다.
- "괜찮아 보인다"는 완료 보고가 아니다.
- 레퍼런스 해석이 갈리거나 의도를 모르면 진행하지 말고 묻는다.
- 틀렸다는 지적을 받으면 **원본부터 다시 연다.** 내 앞선 분석은 증거가 아니다.

`storyboard_scene_market_gen.py` 가 소품 좌표를 역투영한 뒤
`screen_to_world_at_depth` 로 왕복 검증하고 오차가 1e-6m 를 넘으면 생성을
중단하는 것이 이 규칙의 코드판이다.

## 4. 실측 자료가 있으면 그게 우선이다

`storyboard_scene_motorhome.json` 은 사용자가 준 도면으로 만들었다
(전장 10.82m · 폭 2.58m · 실내고 2.13m). 도면이 있으면 가정값은 버린다.
새 씬을 만들 때 실측 자료가 있는지 **먼저 묻는다.**

## 5. 자체 검증을 통과시키고 커밋한다

각 모듈은 bpy 없이 도는 `_demo()` / `--demo` 를 가진다. 전부 통과해야 한다.

```bash
for f in blender_previz/*.py assumptions.py seedance_builder.py; do python "$f"; done
python preview_adapter.py --demo && python preview_render.py --demo
```

버그를 고쳤으면 **그 버그를 잡는 검증을 같이 넣는다.** 카메라 yaw 반전 버그가
통과했던 이유는 자체 검증이 yaw 를 한 번도 확인하지 않아서였다.
